<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.muhammadzain.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AddIngrediante"></activity>
        <activity android:name=".Addinventory" />
        <activity android:name=".GraphActivity" />
        <activity android:name=".Pakistani" />
        <activity android:name=".ReportPage" />
        <activity android:name=".AddUser" />
        <activity android:name=".Login" />
        <activity android:name=".Adder">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".MainActivity">

        </activity>
        <activity
            android:name=".confirmorder"
            android:label="@string/title_activity_confirmorder" />
        <activity
            android:name=".MusaMainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>


</manifest>

